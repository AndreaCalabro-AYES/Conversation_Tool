FROM ubuntu:20.04

RUN apt-get update && apt-get install -y \
    python3 \
    python3-pip \
    libasound2-dev \
    build-essential \
    && apt-get clean

RUN pip3 install --no-cache-dir vosk==0.3.42

WORKDIR /app

COPY . /app

CMD ["python3", "converse.py"]
